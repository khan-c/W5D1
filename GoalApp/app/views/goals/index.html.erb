<h1>Public Goals</h1>

<ul>
  <% @goals.each do |goal| %>
    <li><%= link_to goal.title, goal_url(goal) %>
      <%= button_to "Remove Goal", goal_url(goal), method: :delete %>
    </li>
  <% end %>
</ul>
